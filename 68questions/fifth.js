// a function returns the sum of all the elements inside that array.
  function sumArray(array) {
    let total=0
    for (let num of array) {
      total += num;
    }
    return total;
  }
  let array=[1,2,3];
  console.log(sumArray(array));

  



