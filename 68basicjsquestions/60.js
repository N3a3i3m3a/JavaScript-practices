//unction that calculates the perimeter of a rectangle
function calculateRectanglePerimeter(length, width) {
    return 2 * (length + width);
  }
  
  // Test the function
  const length = 5;
  const width = 4;
  console.log(calculateRectanglePerimeter(length, width));
  